syntax = "proto3";

package geo;

// TODO: Implement me

service Geo {
  rpc Nearby(Request) returns (Result);
}

// The latitude and longitude of the current location.
message Request {
  float lat = 1;
  float lon = 2;
}

message Result {
  repeated string hotelI